{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/dicebear",
      "packages/@dicebear/*",
      "packages/api",
      "apps/*"
    ]
  },
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "prepare-publish": "lerna version --force-publish --exact"
  },
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}
